<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cjc.ouxunmodelpic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewBoxActivity" android:launchMode="singleTop"/>
        <activity android:name=".SearchActivity" android:launchMode="singleTop"/>
        <activity android:name=".SaoMiaoInActivity" android:launchMode="singleTop"/>
        <activity android:name=".ZhuangxiangActivity" android:launchMode="singleTop"/>
        <activity android:name=".zhuangxiangokActivity" android:launchMode="singleTop"/>
        <activity android:name=".ShangsuoActivity" android:launchMode="singleTop"/>
        <activity android:name=".RukuActivity" android:launchMode="singleTop"/>
        <activity android:name=".ChukuActivity" android:launchMode="singleTop"/>
    </application>

</manifest>